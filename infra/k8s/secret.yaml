apiVersion: v1
kind: Secret
metadata:
  name: beer-catalogue-secret
  namespace: default
type: Opaque
# Values are base64-encoded: echo -n 'plaintext' | base64
#
# PLACEHOLDER VALUES ONLY.
# Replace with your real credentials before applying.
data:
  # beeradmin → base64
  DB_USER: YmVlcmFkbWlu
  # change-me → base64  (must match var.db_password in terraform.tfvars)
  DB_PASSWORD: Y2hhbmdlLW1l
