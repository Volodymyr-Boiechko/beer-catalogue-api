apiVersion: v1
kind: Service
metadata:
  name: beer-catalogue
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: beer-catalogue
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
